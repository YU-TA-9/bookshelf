import { css } from '@emotion/react';

export const globalStyle = css`
  html,
  body,
  div,
  p {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  font-family: Roboto;
  font-style: normal;
  font-weight: normal;
`;
